#include<stdio.h>

void func1(int array[], int length)
{
    int sum= 0;
    int product=1;
    for (int i=0; i<length; i++)
    {
        sum+array[1];

    }
    for(int i=0; i<length;i++)
    {
        product*+array[i];
    
    }
}

int main()
{
    int array []= {3,5,66};
    func1(array,3);


    return 0;
    // The program will not compile due to the following errors:
}